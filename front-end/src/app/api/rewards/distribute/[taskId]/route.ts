// app/api/rewards/routes.ts

// POST /api/rewards/distribute/[taskId]
// Distribute rewards for a task
export async function POST(taskId: string) {
  // Uses distributeRewards()
}

// POST /api/rewards/claim/[taskId]
// Claim rewards for a task
export async function POST(taskId: string) {
  // Uses claimReward()
}
